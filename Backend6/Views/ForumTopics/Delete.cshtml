@model Backend6.Models.ForumTopic

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@Model.Name</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Created)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Created)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Creator)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Creator.UserName)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Forum)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Forum.Name)
        </dd>
    </dl>
    
    <form asp-action="Delete" asp-route-forumId="@ViewBag.Forum.Id" asp-route-id="@Model.Id">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-controller="ForumTopics" asp-action="Details" asp-route-forumId="@ViewBag.Forum.Id" asp-route-id="@Model.Id">Back to List</a>
    </form>
</div>
